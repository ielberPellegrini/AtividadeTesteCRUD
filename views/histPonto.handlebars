<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Histórico de Pontos</title>
</head>
<body>
    <h1>Histórico de Pontos</h1>

    <!-- Formulário para pesquisar o histórico por nome -->
    <form action="/historico" method="GET">
        <label for="nomeCompleto">Nome Completo:</label>
        <input type="text" id="nomeCompleto" name="nomeCompleto" required>
        <button type="submit">Pesquisar</button>
    </form>

    <!-- Exibição do histórico filtrado -->
    {{#if historico}}
        <h2>Resultado da Pesquisa para: {{nomeCompleto}}</h2>
        <table border="1">
            <thead>
                <tr>
                    <th>Data</th>
                    <th>Nome Completo</th>
                    <th>Horas Trabalhadas</th>
                    <th>Minutos Trabalhados</th>
                    <th>Horas Excedentes</th>
                </tr>
            </thead>
            <tbody>
                {{#each historico}}
                    <tr>
                        <td>{{this.data}}</td>
                        <td>{{this.nomeCompleto}}</td>
                        <td>{{this.horasTrabalhadas}}</td>
                        <td>{{this.minutosTrabalhados}}</td>
                        <td>{{this.cargaHorariaExcedida}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    {{else}}
        <p>Nenhum resultado encontrado. Tente novamente.</p>
    {{/if}}

    <a href="/">Voltar</a>
</body>
</html>
